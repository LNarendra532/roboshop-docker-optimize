networks:
  default:
    driver: bridge  
    name: roboshop
    # external: true   
volumes:
  mongodb: # Declare the named volume
  redis:
  mysql:
  rabbitmq: # docker volume create rabbitmq

services:
  mongodb:
    image: lvnaren532/mongodb:v1
    container_name: mongodb
    volumes:
    - mongodb:/data/db # moumting /data/db
  catalogue:
    image: lvnaren532/catalogue:v1
    container_name: catalogue
    depends_on:
    - mongodb
  redis:
    image: redis:7.0  # directly iwill pull it form docker hub , so iam not using lvnaren532
    container_name: redis
    volumes:
    - redis:/data
  user:
    image: lvnaren532/user:v1
    container_name: user
    depends_on:
    - redis
    - mongodb
  cart:
    image: lvnaren532/cart:v1
    container_name: cart
    depends_on:
    - catalogue
    - redis
  mysql:
    image: lvnaren532/mysql:v1
    container_name: mysql
    volumes:
    - mysql:/var/lib/mysql
  shipping:
    image: lvnaren532/shipping:v1
    container_name: shipping
    depends_on:
    - mysql
    - cart
  rabbitmq:
    image: rabbitmq:3
    container_name: rabbitmq
    environment:
      RABBITMQ_DEFAULT_USER: roboshop
      RABBITMQ_DEFAULT_PASS: roboshop123
    volumes:
    - rabbitmq:/var/lib/rabbitmq
  payment:
    image: lvnaren532/payment:v1
    container_name: payment
    environment:
      CART_HOST: cart
      CART_PORT: 8080
      USER_HOST: user
      USER_PORT: 8080
      AMQP_HOST: rabbitmq
      AMQP_USER: roboshop
      AMQP_PASS: roboshop123
    depends_on:
    - user
    - rabbitmq
    - cart
  frontend:
    image: lvnaren532/frontend:v1
    container_name: frontend

    ports:
    - "80:80"
    depends_on:
    - catalogue
    - user
    - cart
    - payment
    - shipping
    

    
     
#docker login -u username

    # we manually created a bridge network outside of Compose using the docker [network rosbosohop,
# you can connect your Compose services to it by marking the network as external=true.  


# how to run 'docker compose up [running all] -d [sending to background]'
# docker compose up -d